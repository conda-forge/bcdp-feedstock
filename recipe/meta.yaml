{% set name = "bcdp" %}
{% set version = "0.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a161d79f16e319524ba9e9ea2f308541407cb4a78121996a8f9a2f89806896ef

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - xarray >=0.13.0
    - cartopy
    - requests
    - netCDF4

test:
  imports:
    - bcdp

about:
  home: https://github.com/bcdp/bcdp
  license: Apache-2.0
  license_file: LICENSE
  summary: 'Library for generating climate model evaluation pipelines.'
  dev_url: https://github.com/bcdp/bcdp

extra:
  recipe-maintainers:
    - agoodm
