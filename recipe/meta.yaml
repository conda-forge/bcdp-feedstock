{% set name = "bcdp" %}
{% set version = "0.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 82ebc53ec71a3676874571571bd3a5b97c31beb71a72d26cbd2a9e9011d8871f

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - xarray >=0.13.0
    - cartopy
    - requests
    - netCDF4

test:
  imports:
    - bcdp

about:
  home: https://github.com/bcdp/bcdp
  license: Apache-2.0
  license_file: LICENSE
  summary: 'Library for generating climate model evaluation pipelines.'
  dev_url: https://github.com/bcdp/bcdp

extra:
  recipe-maintainers:
    - agoodm
